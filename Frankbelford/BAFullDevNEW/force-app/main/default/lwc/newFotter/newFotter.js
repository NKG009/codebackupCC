import { LightningElement, wire, track } from 'lwc';
//import USER_ID from '@salesforce/user/Id';
//import getContactRecordType from '@salesforce/apex/ContactHelperCandidate.getContactRecordType';

export default class NewFooter extends LightningElement {
    /*
    contactRecordTypeName;

    @track toplinks = true;
    @track laoded = false;

    @wire(getContactRecordType, { userId: USER_ID })
    wiredContactRecordType({ error, data }) {
        if (data) {
            this.contactRecordTypeName = data;
            // Now you can apply logic based on record type name
            console.log('Contact Record Type:', this.contactRecordTypeName);

            if (this.contactRecordTypeName === '2nd Tier Candidate') {
               this.toplinks = false;
               this.laoded = true;
            }
        } else if (error) {
            console.error('Error fetching contact record type', error);
        }
    }
    */
}